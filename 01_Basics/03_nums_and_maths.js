/* ============= Creating Numbers ============= */

/* let integerNumber = 42;  // Integer (whole number)
let floatNumber = 3.14; */ // Floating-point number (decimal number)

/* ============= Basic Math Operations ============= */

/* let a = 10;
let b = 5;

let addition = a+b;
let subtraction = a - b;
let multiplication = a * b;
let division = a / b;
let modulus = a % b;
let exponentiation = a ** b;

console.log(addition);
console.log(subtraction);
console.log(multiplication);
console.log(division);
console.log(modulus);
console.log(exponentiation); */

/* ============= Math Object in JavaScript ============= */

// JavaScript has a Math object that provides many built-in methods and properties to perform mathematical calculations.

// Common Math Methods

// 1. Math.round(): Rounds a number to the nearest integer.

/* let num1 = 4.7;
let num2 = 4.3;

console.log(Math.round(num1)); //5
console.log(Math.round(num2)); //4

 */

// 2. Math.ceil(): Rounds a number up to the next largest integer.

/* let num = 4.1;
console.log(Math.ceil(num)); */

// 3. Math.floor(): Rounds a number down to the next smallest integer.

/* let num = 6.9;
console.log(Math.floor(num)); */

// 4. Math.random(): Generates a random number between 0 and 1.
/* 
let randomNumber = Math.random();
console.log(randomNumber); // Output: any number between 0 and 1

// Random number between 1 and 10:
let randomNum = Math.floor(Math.random() * 10) + 1;
console.log(randomNum);  // Output: any number between 1 and 10
 */

// 5. Math.sqrt(): Calculates the square root of a number.
/* 
let num = 64;
console.log(Math.sqrt(num));
 */

// 6. Math.abs(): Returns the absolute value of a number. or changes the sign of a number.

/* 
let num = -5;
console.log(Math.abs(num));
 */

// 7. Math.max(): Returns the maximum value among the arguments.

/* 
console.log(Math.max(30, 20, 5, 90));
 */

// 8. Math.min(): Returns the minimum value among the arguments.

/* 
console.log(Math.min(30, 40, 5, 2)); 
 */

// 9. Math.pow(): Calculates the power of a number.

/* 
let base = 2;
let exponent = 3;
console.log(Math.pow(base, exponent)); // Output: 8 (2 raised to the power of 3)
 */

